# HibiscusCommons: Pinac 26.2 migration

## Review before implementation (2026-09-08)

Repository C:/Users/CHESV/Documents/GitHub/Masivo/Cosmetics/HibiscusCommons; clean initial master 8523cbe118bf70a56502ee7c8fc3b097aeb6667f tracking origin/master. Verified origin https://github.com/MasivoSMP/HibiscusCommons.git and remote default master; upstream https://github.com/HibiscusMC/HibiscusCommons.git is read-only. `git fetch origin`, `git pull --ff-only origin master` succeeded (already current); created dev-26.2 with `git switch -c dev-26.2`. No existing migration branch or unrelated changes. No applicable AGENTS.md found in project/parents. Upstream master observed ae2b031fc714b1b640fd5c3825ddc4182f6b7450; no upstream merge is proposed.

Target: accepted Pinac implementation 36efa7fe8c630737216393ca92cf14f1db2c759b, Canvas ddc374bb25e0f1e3cb6836aff89b538cbc10a919, Java 25. Read shared migration reference/progress and C:/Users/CHESV/Documents/GitHub/Pinac-26.2/MIGRATION_26_2.md. Original Masivo/Pinac is not consumed.

Applicable findings:
- onStart -> NMSHandlers.setup -> MinecraftVersion and VERSION_MAP -> reflective NMSUtils/NMSPackets/NMSPacketSender: 26.2 is absent, so startup rejects it. Existing 26.1 adapter includes channel intercepts, inventory/equipment/passengers/scale, entity wrappers, item colors and advancement toast internals; it must compile against actual 26.2, not be blindly selected.
- Preserve older adapters and public packet interfaces. Add v26_2_R1 following established adapter layout and explicit 26.2 routing. Keep common Java 21 bytecode where possible, compile common against target API to catch Adventure 5 removals; new internals adapter uses Java 25.
- Gradle 9.4.1 is already suitable. Use Weaver userdev 2.4.5 for the new adapter and io.canvasmc.pinac:dev-bundle:26.2-local with module metadata; common consumes io.canvasmc.pinac:pinac-api:26.2-local. No second Paper API on the target adapter. Existing HMCC_INCLUDE_HASH suppression is retained.
- Public references: [Paper 26.2 changes](https://papermc.io/news/26-2/) identify Adventure 5 and version-format changes; [Canvas userdev setup](https://docs.canvasmc.io/canvas/developers/plugins/userdev/) documents bundle consumption and avoiding duplicate APIs. Exact signature evidence will come from pinned target compilation/source inspection.
- ItemSerializer, ItemBuilder, Adventure helpers and optional hooks require target compilation and old-item/client validation. No raw vanilla player/world file migration, bed PDC migration, cube event handling or generated resource-pack change identified. Plugin-owned configuration/data keys and scheduling/packet contracts remain unchanged.

Plan: add and port only the 26.2 adapter/routing and build dependencies; retain all legacy support. Run target/full packaging checks, focused no-framework version regression, inspect archive/classes/resolution and record hashes. Commit coherent checkpoints and explicitly push only HEAD:refs/heads/dev-26.2. No Maven-local publication without orchestrator coordinate/lock approval. HMCCosmetics is a separate read-only consumer; it must later build against this provider.

Runtime gates (not run): isolated Pinac startup, channel injection/reflection, inventory cursor/state and equipment/passenger/scale packets, NPC/team/metadata/client rendering, toast advancement lifecycle, item round-trip using copied 1.21.11 data, optional hooks present/absent and Folia ownership/concurrency. Build success alone is not deployment certification.

## Implementation evidence

- New v26_2_R1 follows all 26 source files of the existing 26.1 adapter. Target compiler identified nine errors: EntityType.ARMOR_STAND moved to EntityTypes.ARMOR_STAND (eight call sites), and Entity.nextEntityId() moved to ServerLevel.getNextEntityId(). Target source evidence: pinac-server/src/minecraft/java/net/minecraft/world/entity/EntityTypes.java:186 and server/level/ServerLevel.java:815. The latter still uses a static AtomicInteger; the no-argument public Commons contract is retained by using the overworld allocator. All Commons/HMCCosmetics callers were inspected; no consumer source was edited. Cross-region allocation and fake-entity wrappers remain runtime gates.
- Common target compile removed the accidental javax.annotation.Nullable transitive dependency by using the already declared JetBrains annotation. Common retains release 21 with Java 25 compiler/disableAutoTargetJvm, matching the existing mixed-version build pattern. No Adventure source adaptation was required by target compilation.
- Version parsing now tolerates versions without a hyphen; existing .build and legacy suffix handling remain. A no-framework Gradle :common:migrationCheck exercises target/local/build/legacy versions, unknown-version rejection, ordering and adapter registration without starting Bukkit.
- Existing api-version 1.21, Folia flag, old adapters, configuration keys, optional hooks, public signatures, packet actions, toast persistence behavior and HMCC_INCLUDE_HASH suppression are retained. The development runServer task still targets its inherited 1.21.8 fixture; it is not a Pinac validation command and was not run.
- Pre-existing adapter limitations were not expanded into an unrelated rewrite: shared mutable fake-entity wrappers, the slot-update wrapper's index initialization and raw packet callbacks require runtime review. ItemSerializer.deserialize retains its PDC/config interpretation; its pre-existing serialize method is empty. Do not interpret compilation as proof of old-item persistence or live concurrency correctness.

Verified local input SHA-256: API 1952d473fb3a4b57a227962759ac8776b8e28eacda000196ef62609f8239b4ee; development bundle 57966f9b9993fd2950dfa067d48ba39ae4329389b26cfbd69cdec655863d1c5b. No upstream/server sources changed.

## Validation and deliverable

All commands ran in this repository with JAVA_HOME=C:/Users/CHESV/.gradle/jdks/eclipse_adoptium-25-amd64-windows.2 and HMCC_INCLUDE_HASH=false. No deployment/server startup or Maven publication ran.

- `gradlew.bat :v26_2_R1:compileJava --no-configuration-cache --console=plain`: initial attempts exposed JVM variant selection, missing javax annotation and moved NMS symbols; corrected as above. Logs: build/migration-26.2/target-compile*.log.
- `gradlew.bat build :common:migrationCheck --no-configuration-cache --console=plain`: all seven adapters compiled and shaded packaging completed; check passed, but common:test rejected the standalone main as an undiscovered framework test (7m10s, build/migration-26.2/build-26.2.log). Final fix places the assertion check in its own migrationCheck source set/JavaExec task; no test-discovery failure is suppressed, and normal test semantics remain intact.
- `gradlew.bat build :common:migrationCheck :v26_1_R1:dependencies --configuration paperweightDevelopmentBundle --no-configuration-cache --console=plain`: PASS, 4s, 32 tasks / 6 executed, build/migration-26.2/final-build.log. Existing framework test tasks report NO-SOURCE; the standalone version/regression task executes and passes. All target/legacy modules had compiled in the preceding full build; no failed source compilation remains.
- Dependency inspection command: `gradlew.bat build :common:migrationCheck :common:migrationDependencies :v26_2_R1:migrationDependencies -I build/migration-26.2/dependencies.init.gradle --no-configuration-cache --console=plain` (PASS; build/migration-26.2/verified-build.log). The local diagnostic init prints resolved compile artifacts. Common and target adapter each resolve only Pinac API 26.2-local, no Paper API duplicate; Adventure is 5.2.0. Target bundle selects Authlib 9.0.75. Common retains unused inherited Authlib 3.13.56 compile-only declaration. Existing legacy 26.1 selector resolves 26.1.2.build.74-stable; no unrelated legacy dependency upgrade/pin rewrite was made.
- Archive inspection with System.IO.Compression.ZipFile: seven adapter NMSUtils classes present; 28 new 26.2 classes including generated inner classes; no org/bukkit, net/minecraft or net/kyori/adventure classes; no MigrationCheck packaged. plugin.yml retains api-version 1.21 / Folia true and plugin version 0.9.1 (hash suppression). `javap -verbose` verifies common class major 65 (Java 21), target adapter major 69 (Java 25), and ServerLevel.getNextEntityId call.
- `javap -public` plus Compare-Object against the existing output/HibiscusCommons-0.9.1-8523cbe.jar: identical signatures for NMSUtils, NMSPacketBuilder, NMSPacketSender, PacketInterface, ServerUtils and HibiscusCommonsAPI (build/migration-26.2/public-api.txt). MinecraftVersion only adds v26_2. This is source/public-signature and compile coverage, not old-server runtime certification; common was compiled against Adventure 5.
- `git diff --check`: PASS. Existing deprecation/Gradle 10/native-access warnings remain, with no ignored validation failure in final build.

Consumer-ready shaded JAR (1,323,479 bytes):
C:/Users/CHESV/Documents/GitHub/Masivo/Cosmetics/HibiscusCommons/output/HibiscusCommons-0.9.1.jar
SHA-256: bf5e810696b7622c3dc9c5376be16881971e04079ddd35b18acded79f5ae642e
Identical copy: C:/Users/CHESV/Documents/GitHub/Masivo/Cosmetics/HibiscusCommons/build/libs/HibiscusCommons-0.9.1.jar.

Per orchestrator direction, hand off this hashed shaded JAR; preserve the existing Maven publication workflow. HMCCosmetics currently requests me.lojosho:HibiscusCommons:0.9.1-8523cbe; that coordinate has NOT been replaced and is not this migrated artifact. Its owner must consume the approved path/hash or coordinate a scoped publication later. No new publication metadata/coordinate was introduced. No sibling plugin or central tracker was edited.

Final status: compile/build/regression/package inspection PASS. Runtime/client/old-data/optional-hook/Folia gates listed above NOT RUN; no runtime pass or deployment readiness claimed. No observed remaining build blocker. Known inherited adapter limitations remain explicit runtime/review risks, not newly verified failures.
